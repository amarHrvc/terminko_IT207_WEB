<?php


// This file is only for IDE autocomplete and should not contain any logic.

use App\Controller\BookingController;
use App\Controller\RatingController;
use App\Controller\ServiceController;
use App\Controller\TenantController;
use App\Controller\UserController;
use App\Dao\BookingDao;
use App\Dao\RatingDao;
use App\Dao\ServiceDao;
use App\Dao\TenantDao;
use App\Dao\UserDao;

/**
 * @method static UserController UserController() Resolves the UserController instance.
 * @method static BookingController BookingController() Resolves the BookingController instance.
 * @method static RatingController RatingController() Resolves the RatingController instance.
 * @method static ServiceController ServiceController() Resolves the ServiceController instance.
 * @method static TenantController TenantController() Resolves the TenantController instance.
 *
 * @method static BookingDao BookingDao() Resolves the TenantController instance.
 * @method static RatingDao RatingDao() Resolves the TenantController instance.
 * @method static ServiceDao ServiceDao() Resolves the TenantController instance.
 * @method static TenantDao TenantDao() Resolves the TenantController instance.
 * @method static UserDao UserDao() Resolves the TenantController instance.
 *
 */

class Flight
{
    // This file is only for IDE autocomplete and should not contain any logic.
}
